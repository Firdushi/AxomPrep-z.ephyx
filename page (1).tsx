import Link from 'next/link';
import { createClient } from '@/lib/supabase/server';
import EmptyState from '@/components/EmptyState';
export default async function Home(){const supabase=await createClient();const [{data:notes},{data:tests},{data:cats}]=await Promise.all([supabase.from('notes').select('id,slug,title_en,excerpt_en').eq('published',true).order('created_at',{ascending:false}).limit(3),supabase.from('tests').select('id,title_en,description_en,duration_minutes').eq('published',true).order('created_at',{ascending:false}).limit(3),supabase.from('categories').select('id,name_en,name_as').order('name_en').limit(8)]);return <><section className="hero"><div className="container hero-grid"><div><span className="eyebrow">Assam-focused learning platform</span><h1>Learn. Practice. Prepare.</h1><p>Study notes, MCQs, mock tests, Assam GK and exam-focused resources built for learners across Assam.</p><form className="searchbar" action="/search"><input className="input" name="q" placeholder="Search notes, subjects and topics…" aria-label="Search"/><button className="btn btn-primary">Search</button></form></div><div className="hero-card"><span className="pill">AxomPrep</span><div className="big">A+</div><h2>Build confidence through practice.</h2><p>Track attempts, bookmark useful resources and prepare at your own pace.</p><Link className="btn btn-primary" href="/tests">Explore mock tests</Link></div></div></section><section className="section"><div className="container"><div className="section-head"><div><h2>Featured study materials</h2><p className="muted">Clear, focused resources for everyday learning.</p></div><Link href="/notes">View all</Link></div>{notes&&notes.length?<div className="grid grid-3">{notes.map(n=><Link className="card" href={`/notes/${n.slug}`} key={n.id}><span className="pill">Study Note</span><h3>{n.title_en}</h3><p className="muted">{n.excerpt_en}</p></Link>)}</div>:<EmptyState/>}</div></section><section className="section"><div className="container"><div className="section-head"><div><h2>Mock tests</h2><p className="muted">Timed practice with secure server-side scoring.</p></div><Link href="/tests">All tests</Link></div>{tests&&tests.length?<div className="grid grid-3">{tests.map(t=><Link className="card" href={`/tests/${t.id}`} key={t.id}><span className="pill">{t.duration_minutes} min</span><h3>{t.title_en}</h3><p className="muted">{t.description_en}</p></Link>)}</div>:<EmptyState title="No published tests yet" text="Admins can publish tests from the admin dashboard."/>}</div></section><section className="section"><div className="container"><div className="section-head"><div><h2>Explore categories</h2><p className="muted">Find content by subject and exam area.</p></div></div>{cats&&cats.length?<div className="grid grid-3">{cats.map(c=><Link className="card" href={`/notes?category=${c.id}`} key={c.id}><h3>{c.name_en}</h3><p className="muted">{c.name_as}</p></Link>)}</div>:<EmptyState title="Categories coming soon"/>}</div></section><section className="section"><div className="container card" style={{textAlign:'center'}}><h2>Ready to prepare smarter?</h2><p className="muted">Create an account to take tests, save bookmarks and track your progress.</p><Link className="btn btn-primary" href="/signup">Create free account</Link></div></section></>}
