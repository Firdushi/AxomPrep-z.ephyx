import Link from 'next/link';import {requireAdmin} from '@/lib/auth';import DeleteQuestionButton from '@/components/DeleteQuestionButton';
export default async function Questions(){const {supabase}=await requireAdmin();const {data}=await supabase.from('questions').select('id,question_en,difficulty,categories(name_en),created_at').order('created_at',{ascending:false}).limit(200);return <div><div className="section-head"><div><h1>Questions</h1><p className="muted">Manage the MCQ bank.</p></div><div className="actions"><Link className="btn btn-secondary" href="/admin/questions/import">Import CSV</Link><Link className="btn btn-primary" href="/admin/questions/new">New question</Link></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Question</th><th>Category</th><th>Difficulty</th><th>Actions</th></tr></thead><tbody>{data?.map(q=><tr key={q.id}><td>{q.question_en}</td><td>{q.categories?.name_en}</td><td>{q.difficulty}</td><td><div className="actions"><Link className="btn btn-secondary" href={`/admin/questions/edit/${q.id}`}>Edit</Link><DeleteQuestionButton id={q.id}/></div></td></tr>)}</tbody></table></div></div>}
